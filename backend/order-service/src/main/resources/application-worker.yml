# Worker instances own background execution and expose no fixed service port.
server:
  port: ${WORKER_SERVER_PORT:0}
management:
  server:
    port: ${WORKER_MANAGEMENT_PORT:0}
mypet:
  scheduling:
    role: ${MYPET_SCHEDULING_ROLE:WORKER}
